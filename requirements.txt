# Core simulation & experiment dependencies
simpy>=4.1.0
networkx>=3.0
matplotlib>=3.7.0
aiohttp>=3.9.0
numpy
pandas
python-docx>=1.1.0
# mathml2omml unpinned: latest (0.1.x) needs py>=3.12; DGX py3.11 installs 0.0.2.
# Generator probes the API at build time and falls back to PNG equations if absent.
mathml2omml
latex2mathml
pytest>=7.0.0
wandb>=0.16.0
